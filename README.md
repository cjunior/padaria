# Damas

Jogo de damas em HTML, CSS e JavaScript puro para jogar contra o computador.

## Rodar localmente

Abra `index.html` no navegador.

## Rodar com Docker

```bash
docker build -t damas .
docker run --rm -p 8080:80 damas
```

Depois acesse `http://localhost:8080`.
